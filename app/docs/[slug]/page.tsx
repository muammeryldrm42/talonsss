import Link from 'next/link'; import { docsContent, docsNav } from '@/lib/data'; import { notFound } from 'next/navigation';
const slugMap:Record<string,string>={'Get Started':'get-started','AI Builder':'ai-builder','Skills':'skills','Examples':'examples','Components':'components','Templates':'templates','Prompting Guide':'prompting-guide','Solana Use Cases':'solana-use-cases','Preview Rules':'preview-rules','File Structure':'file-structure','Troubleshooting':'troubleshooting'};
export function generateStaticParams(){return Object.keys(docsContent).map(slug=>({slug}))}
export default async function DocsPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params; const page=docsContent[slug]; if(!page) notFound(); return(<div className="mx-auto grid max-w-7xl gap-8 px-4 py-10 sm:px-6 lg:grid-cols-[260px_minmax(0,1fr)_220px] lg:px-8"><aside className="glass h-fit rounded-[24px] border border-line p-4"><div className="mb-4 flex items-center gap-3 border-b border-line pb-4 text-sm font-semibold tracking-[0.18em] text-text"><span className="text-xl">🦅</span>TALONS</div><div className="space-y-1">{docsNav.map(item=>{const itemSlug=slugMap[item];const active=itemSlug===slug;return(<Link key={item} href={`/docs/${itemSlug}`} className={`block rounded-2xl px-3 py-2 text-sm transition ${active?'bg-white/[0.06] text-text':'text-muted hover:bg-white/[0.03] hover:text-text'}`}>{item}</Link>)})}</div></aside><article className="min-w-0"><div className="mb-3 text-xs font-semibold uppercase tracking-[0.24em] text-brand2">Documentation</div><h1 className="text-4xl font-semibold text-text sm:text-5xl">{page.title}</h1><p className="mt-5 max-w-3xl text-lg leading-8 text-muted">{page.intro}</p><div className="mt-10 space-y-8">{page.sections.map(s=><section key={s.title} className="glass rounded-[24px] border border-line p-6"><h2 className="text-2xl font-semibold text-text">{s.title}</h2><p className="mt-3 leading-8 text-muted">{s.body}</p></section>)}</div></article><aside className="glass h-fit rounded-[24px] border border-line p-4 text-sm text-muted"><div className="mb-4 font-semibold text-text">On this page</div><div className="space-y-2">{page.sections.map(s=><div key={s.title}>{s.title}</div>)}</div></aside></div>)}
