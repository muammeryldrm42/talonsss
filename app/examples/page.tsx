import Link from 'next/link'; import { ArrowRight } from 'lucide-react'; import { exampleCards } from '@/lib/data';
export default function ExamplesPage(){return(<div className="mx-auto max-w-7xl px-4 py-14 sm:px-6 lg:px-8"><div className="mb-10 max-w-3xl"><div className="mb-3 text-xs font-semibold uppercase tracking-[0.24em] text-brand2">Examples</div><h1 className="text-4xl font-semibold text-text sm:text-5xl">Ready-made Solana product surfaces</h1><p className="mt-4 text-base text-muted">Open any example in Builder and continue shaping the output with your own prompt.</p></div><div className="grid gap-5 md:grid-cols-2 xl:grid-cols-3">{exampleCards.map(e=><div key={e} className="glass rounded-[24px] border border-line p-5"><div className="mb-4 rounded-[20px] border border-line bg-gradient-to-br from-brand/15 via-black/20 to-brand2/10 p-4"><div className="h-40 rounded-2xl border border-white/10 bg-black/25"/></div><h2 className="text-xl font-semibold text-text">{e}</h2><p className="mt-2 text-sm text-muted">Use this as a starting point inside the Builder workspace.</p><Link href={`/builder?prompt=${encodeURIComponent(`Build a premium ${e.toLowerCase()} for Solana with a dark premium UI, polished states, and production-ready structure.`)}`} className="mt-5 inline-flex items-center gap-2 text-sm font-medium text-brand2 hover:text-white">Open in Builder <ArrowRight className="h-4 w-4"/></Link></div>)}</div></div>)}
