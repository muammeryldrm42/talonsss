"use client";
import { useMemo, useState } from 'react'; import { ChevronDown, Sparkles } from 'lucide-react'; import { PromptTopic, promptTopics } from '@/lib/data'; import { useRouter } from 'next/navigation';
export function PromptPanel(){const [expanded,setExpanded]=useState(false);const [prompt,setPrompt]=useState('');const router=useRouter();const visible=useMemo<PromptTopic[]>(()=>expanded?promptTopics:promptTopics.slice(0,4),[expanded]);const submit=(value:string)=>{const finalPrompt=value.trim()||'Build a premium Solana protocol dashboard with KPI cards, a treasury overview, validator metrics, and a dark glassmorphism layout.';router.push(`/builder?prompt=${encodeURIComponent(finalPrompt)}`)};return(<div className="glass rounded-[28px] border border-line p-4 shadow-glow sm:p-6"><div className="grid gap-4"><textarea value={prompt} onChange={e=>setPrompt(e.target.value)} placeholder="Describe the Solana app, page, component, or system you want to build..." className="min-h-36 w-full rounded-[24px] border border-line bg-black/20 px-5 py-4 text-base text-text outline-none placeholder:text-slate-500 focus:border-brand/80"/><div className="flex flex-col gap-4 lg:flex-row lg:items-center lg:justify-between"><div className="flex flex-wrap gap-3">{visible.map(t=><button key={t.id} onClick={()=>submit(t.short)} className="rounded-full border border-line bg-white/[0.03] px-4 py-2 text-sm text-slate-200 transition hover:border-brand2/60 hover:text-white">{t.label}</button>)}<button onClick={()=>setExpanded(c=>!c)} className="inline-flex items-center gap-2 rounded-full border border-line bg-white/[0.03] px-4 py-2 text-sm text-muted"><ChevronDown className={`h-4 w-4 transition ${expanded?'rotate-180':''}`}/>{expanded?'Show less':'Show more'}</button></div><button onClick={()=>submit(prompt)} className="inline-flex items-center justify-center gap-2 rounded-2xl bg-white px-4 py-3 text-sm font-semibold text-slate-950"><Sparkles className="h-4 w-4"/>Generate</button></div></div></div>)}
